export enum RoutePath {
  Root = "/",
  CreateWallet = "/create",
  RestoreWallet = "/restore",
  Balances = "/balances",
  Mnemonic = "/mnemonic",
  Password = "/password",
  Send = "/send",
  Addresses = "/addresses",
  ConfirmTransaction = "/confirm-transaction",
}
